import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moneyui.app',
  appName: 'Money App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
